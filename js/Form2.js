class Form2
{
  /*
  name
  inputbox
  enter key
  input box
  button{
    button is pressed
    database.ref("/"+key.value()+"/PLAYERCOUNT").read((data)=>
    {
      var checkRandomkey = data.val()
    });
    if(!checkRandomKey) //!undefined
  
    {
      player = new Player();
    }
        check the the value key, store the value, check in the database

  }

}